package net.osmand.plus.search;

/** Runtime configuration for the optional Google address search provider. */
public final class GoogleSearchSettings {
    public enum ProviderMode { OSMAND_ONLY, GOOGLE_ONLY, BOTH }
    private GoogleSearchSettings() {}
}
